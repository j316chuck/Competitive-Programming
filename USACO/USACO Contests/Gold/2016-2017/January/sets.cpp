#include <iostream>
#include <set>
using namespace std;

int main(){
    /*set<int> st;
    st.insert(1);
    st.insert(2);
    set<int> sets;
    sets.insert(2);
    sets.insert(3);
    set<int> sv = st | sets;
    for(auto it = sv.begin(); it!=sv.end(); it++){
        cout<<it*<<endl;
    }*/
}

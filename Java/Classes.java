public class Classes{
	public static void main(String[] args){

	}
}

/*
classes have instance variables which are its own variables (mydog.food mydog.bark)
classes also have nonstatic methods/instance methods which you can call on itself (mydog) just public
it also has static methods which can only be referenced by the class Dog 
(class methods vs instance methods)
also static variables. Variables inherent to the class itself. Dog.binomen vs mydog.binomen

declaration Dog d;
assignment d = new Dog(0); instantiation
Construction (goes to public class);

public Dog(int w){
	
}
can use this to create one with different parameters
*/

/*Integer.parseInt(args[0])*/
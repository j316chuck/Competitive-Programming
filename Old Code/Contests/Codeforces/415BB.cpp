 //priority_queue<int, vector<int>, greater<int>> pq;
    /*for (int i = 0; i < n; i++) {
        scanf("%lld %lld", &k, &l);
        pq.push(min(2 * k, l));
        if (pq.size() > f) {
            pq.
            pq.pop();
        }
    }
    long long total = 0;
    while (!pq.empty()) {
        total += pq.top();
        pq.pop();
    }
    printf("%lld", total);*/

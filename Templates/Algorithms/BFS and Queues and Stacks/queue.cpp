#include <iostream>
#include <cstdio>
#include <algorithm>
#include <cstring>
#include <string>
#include <cctype>
#include <stack>
#include <queue>
#include <list>
#include <vector>
#include <map>
#include <sstream>
#include <cmath>
#include <bitset>
#include <utility>
#include <set>
#include <numeric>
#include <ctime>
#include <climits>

const double Pi=acos(-1.0);
typedef long long LL;

#define Set(a, s) memset(a, s, sizeof (a))
#define Rd(r) freopen(r, "r", stdin)
#define Wt(w) freopen(w, "w", stdout)
#define MAX 1001
using namespace std;

int main(){

    /*set<int> mySet; //unique values
    mySet.insert(100);
    size_t num = mySet.size();
    //cout<<num<<endl;
    set<int>::iterator it = mySet.find(100);
    cout<<*it<<endl;*/

    /*map<string, int> myIDMap;
    myIDMap["patrick"] = 100;
    size_t num = myIDMap.size();
    cout<<myIDMap["patrick"]<<endl;
    map<string, int>::iterator it;
    for(it = myIDMap.begin(); it!= myIDMap.end(); it++){
        cout<<(*it).first<<' '<<(*it).second<<endl;
    }*/
    //
    /*queue<int> myQ;
    myQ.push(0);
    myQ.push(1);
    myQ.push(2);
    myQ.push(3);
    int dV = myQ.front();
    myQ.pop();
    if (myQ.empty())
        cout<<"aslkdjaskl"<<endl;
    int back = myQ.back();
    /*queue<int>::iterator it;
    for(it=myQ.begin(); it != myQ.end(); it++){
        cout<<(*it)<<endl;
    }*/




}
